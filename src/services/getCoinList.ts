/**
 * Retrieves a list of cryptocurrencies.
 * @returns A promise that resolves to an array of Coin objects.
 */
type Coin = { id: string; symbol: string; name: string };
// essa função será responsável por retornar uma lista de criptomoedas,
async function getCoinList(): Promise<Coin[]> {
  //
}
